"""Audio capture and playback, always routed to a named device profile
(see devices.py) — never PipeWire's own 'default', which silently
changes on this machine between earbuds and the built-in card.
"""
